import 'package:flutter/material.dart';

class TutorSignup extends StatelessWidget {
  const TutorSignup({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text("Signup for Tutor"),
    );
  }
}
