import 'package:flutter/material.dart';

class TutorLogin extends StatelessWidget {
  const TutorLogin({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text("Login for Tutor"),
    );
  }
}
